{
  "name": "learnmcp",
  "version": "0.0.0",
  "private": true,
  "description": "A dynamic, in-session LMS for building with LLMs — pluggable cartridges that reward best practices with badges.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
