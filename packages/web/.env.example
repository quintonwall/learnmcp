# Copy to .env.local and fill in to enable the live registry + leaderboard.
# Without these, the app runs on the bundled first-party cartridges (no backend needed).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
